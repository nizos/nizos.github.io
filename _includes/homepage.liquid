<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ title }}</title>
    </head>
    <body>
        <h1>Blog posts</h1>
        <ul>
            {%- for post in collections.posts reversed -%}
            <li>
                <a href="{{ post.url }}">{{ post.data.title }}</a>
            </li>
            {%- endfor -%}
        </ul>
    </body>
</html>