<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'sha256-bnsXgoBMUJcSIAsILZOImI+J0/8K+7agH+JdhE/1BJvI='; style-src 'self'; img-src 'self'; connect-src 'self' https://plausible.io/api/event; manifest-src 'self';">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="/assets/css/main.css" rel="stylesheet" />

        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
        <link rel="manifest" href="/site.webmanifest">

        {% assign desc = description %}
        {% if desc == blank %}
            {% assign desc = site.description %}
        {% endif %}
        {% if desc %}
            <meta name="description" content="{{ desc | escape  }}">
            <meta property="og:description" content="{{ desc | escape }}">
            <meta name="description" content="{{ desc | escape }}"/>
        {% endif %}

        {% if socialImage == blank %}
            {% assign socialImage = site.defaultSocialImage %}
        {% endif %}
        {% if socialImage %}
            <meta property="og:image" content="{{ socialImage | escape }}"/>
            <meta name="twitter:image" content="{{ socialImage | escape }}"/>
        {% endif %}

        <title>
            {% if title == site.name %}
                {{ site.title | escape }}
            {% else %}
                {{ title | escape }} | {{ site.name | escape}}
            {% endif %}
        </title>

        <meta property="og:site_name" content="{{ site.name }}">
        <meta property="og:type" content="website">
        <script defer data-domain="nizar.se" src="https://plausible.io/js/script.js" integrity="sha256-nsXgoBMUJcSIAsILZOImI+J0/8K+7agH+JdhE/1BJvI=" crossorigin="anonymous"></script>
    </head>
    <body>
        <div class="layout-wrapper">
            {% include 'partials/header' %}
            <main class="main">
                {{ content }}
            </main>
            {% include 'partials/footer' %}
        </div>
    </body>
</html>